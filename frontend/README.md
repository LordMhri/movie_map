# Movie Map frontend

The React interface for searching and exploring the generated movie map.

```bash
npm install
npm run dev
```

The app expects the API at `http://localhost:8080`. Override it when needed:

```bash
VITE_API_URL=https://example.test npm run build
```
